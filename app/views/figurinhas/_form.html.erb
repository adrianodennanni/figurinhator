<%= form_for(:figurinha, html: { class: "form-horizontal", role: "form" }, url: figurinhas_path) do |f| %>
  <% if @figurinha.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@figurinha.errors.count, "error") %> prohibited this figurinha from being saved:</h4>

      <ul>
      <% @figurinha.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label 'Digite aqui suas figurinhas, separardas por espaço:', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= text_area :text_numeros, '', class: "form-control", rows: 3 %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
